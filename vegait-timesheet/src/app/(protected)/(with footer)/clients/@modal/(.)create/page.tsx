export default async function CreateClientModal() {
	return (
		<div>TEST</div>
		// <ModalWrapper>
		// 	<Modal isOpen={true} returnUrl={APP_ROUTES.CLIENTS}>
		// 		<h2>Create existing Client</h2>
		// 	</Modal>
		// </ModalWrapper>
	);
}
